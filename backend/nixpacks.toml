[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python manage.py migrate && python manage.py seed_data && python manage.py seed_courses_full && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"
